// Shared decorative palette for the auth flow's dark "hero" header (Login,
// Signup, ForgotPassword, EmailLinkSent, EmailLinkAuth). Deliberately fixed
// (not theme-reactive) — the hero stays dark regardless of light/dark mode,
// matching the warm-ink brand accent used for HomeHub's primary CTAs.
export const HERO_DARK = '#1E2119';
export const HERO_SHAPE = '#2A2E24';
